# 拖动与启动定位修复

版本：1.0.1（Android versionCode 2）

APK 构建成功：0 个构建错误、5 个警告。输出文件 `Builds/Android/NeuroPilotXR_MR_DragFix.apk`，大小 44,900,066 字节，已核对包名 `com.neuropilot.xr` 与版本 1.0.1。

SHA256：`6D42535237BB5E1CC52DE7720EE4E17C9C7F6B62B1AEF7F425ADAB2094E1C9F5`

## 操作变化

1. 左手或右手射线指向窗口顶部，按住食指扳机，移动手柄即可在空间中拖动窗口；松开后停留。顶部新增“按住扳机拖动”提示。
2. 每次进入入口场景，等待头显位置有效后，将窗口放在当前水平方向前方 1.5 米、眼睛下方 0.10 米处。正常转头时窗口不跟随；从后台恢复入口时重新定位。

旧版抓取使用侧握键，而按钮使用食指扳机，因此出现能感觉到悬停震动、扳机却不能拖动的情况。本次在导航场景统一了这两个动作。原先写死的世界坐标已改成启动时按当前头显计算。

## 已执行的验证

- Unity 2022.3.62f1c1 编译通过；导航场景无丢失脚本或损坏引用。
- 用虚拟左右手控制器发送 Input System 输入，经过实际绑定、XR Ray Interactor 和 XR Grab Interactable：双方均成功悬停、扳机选中、三维拖动（实测约 0.30 米且包含前后深度）、松手后固定。
- 同样通过手柄输入点击“进入训练”，左右手均正确翻页，没有误抓窗口。
- 16 组初始姿态计算检查通过：不同世界位置、0/90/180/270 度朝向，以及俯仰到正上/正下，仍保持窗口水平距离和竖直朝向。
- Play 模式从错误的旧位置重新定位成功；等待时隐藏 UI 并禁用抓取，放置后恢复。
- Play 模式改变测试观察者的位置、身高和朝向后，已放置窗口不跟随；模拟暂停恢复后出现在新观察者正前方。

回归工具：`Assets/NeuroPilot/Editor/SpatialWindowInteractionVerification.cs`。在 Play 模式调用 `NeuroPilotXR.Editor.SpatialWindowInteractionVerification.Begin()`，通过 `Status()` 查看结果。该文件只参与编辑器，不进入 APK。

## 头显最后验收

当前电脑没有连接头显，以上为编辑器输入和生命周期模拟验证。

安装 `Builds/Android/NeuroPilotXR_MR_DragFix.apk` 后：先拖到侧面，退出应用；换一个朝向重开，确认出现在新的正前方。再分别用左右手扳机拖动，并测试松手固定及按钮点击。实际佩戴时的舒适度和 Focus Vision 暂停/恢复行为仍以真机为准。

完整安装步骤见同目录的《新手操作教程.md》。
