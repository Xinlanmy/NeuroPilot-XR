# NeuroPilot XR 1.1 场景接入与验收

## 本版变化

“开始训练”现在进入用户提供的 TrainingRoom，而不是星际占位场景。源项目为开发机 `D:\workspace\11`，原文件夹未修改；只导入训练场景、运行脚本和配置，没有覆盖主项目的 Packages、XR 设置或导入源项目的自动场景生成器。

- 保留入口 UI、空间拖动、每次启动在正前方摆放和 MR 淡出转场。
- 训练房间使用主项目的 OpenXR/XRI 手柄和头显配置；进入后对齐训练房间朝向，保留真实眼高。
- 原材质转换为 URP，修正中文 HUD 字体、尺寸和朝向。
- 保留源配置：一轮 180 秒，准备 3 秒，配置闪烁频率 15 Hz、占空比 0.5，单球最长 12 秒。
- 强度选择会带入房间，但本版三个强度使用同一套原配置，未实现强度参数分级。
- 真实 EEG 尚未接入。扳机/空格只用于模拟一次命中，不代表脑机识别。

## 安装到头显：按顺序做

1. 打开仓库的 [v1.1.1 Release](https://github.com/Xinlanmy/NeuroPilot-XR/releases/tag/v1.1.1)，在 Assets 中下载 `NeuroPilotXR_1.1.1_TrainingRoom.apk`。私有仓库需要登录有权限的 GitHub 账号。
2. 打开头显开发者模式，用 USB 接电脑，在头显里允许 USB 调试。
3. 打开 PowerShell；以下为开发机示例路径，其他电脑替换为自己的 adb 和 APK 路径：

```powershell
& 'D:\UnityAndroidTools\2022.3\SDK\platform-tools\adb.exe' devices
& 'D:\UnityAndroidTools\2022.3\SDK\platform-tools\adb.exe' install -r 'D:\workspace\NeuroPilotXR\Builds\Android\NeuroPilotXR_1.1.1_TrainingRoom.apk'
```

4. 第一条命令应显示设备并标记 `device`；如果是 `unauthorized`，在头显里确认授权再重试。
5. 第二条显示 `Success` 后，在头显应用列表打开 NeuroPilot XR。
6. 如果提示签名不兼容，不要直接卸载：先确认旧版是否有需要保留的数据，再决定处理方式。

## 使用和验收

1. 启动后，入口窗口出现在眼前约 1.5 米处；指向顶部蓝条，按住扳机拖动，松手固定。
2. 点“进入训练”，选择强度，再点“开始训练”。
3. 倒计时后真实环境淡出，进入白色房间；训练房间还有原逻辑的 3 秒准备时间。
4. 确认画面没有粉色材质，只有一个头显视角，计时/命中/漏失和中文提示可读。
5. 球出现后按一次扳机：模拟一次命中、球变色，随后出下一球。持续按住不应跨球自动连击。不需要用射线点中球。
6. 不按扳机，超过原配置的 12 秒应计一次漏失。
7. 180 秒结束后显示结算；松开再按侧握键可重新开始。电脑 Play 模式用空格命中、R 重开。
8. 退出应用后换个朝向重新打开，确认入口仍在当前正前方，而非上次拖动的位置。

## 在 Unity 中打开和构建

1. Unity Hub 打开本仓库根目录，使用项目指定的 Unity 2022.3.62f1c1。
2. 打开 `Assets/NeuroPilot/Scenes/NeuroPilotNavigation.unity`，点击 Play 可验证流程（电脑无真实 MR 透视）。
3. `File > Build Settings` 的 Scenes In Build 应只有两项，且均勾选：入口导航第 0 项；`Assets/NeuroPilot/TrainingRoom/Scenes/TrainingRoom.unity` 第 1 项。
4. 平台选 Android；Player Settings 中 Version 为 `1.1.1`，Bundle Version Code 为 `4`。
5. 点击 Build，保存为 `NeuroPilotXR_1.1.1_TrainingRoom.apk`。

如需重新执行场景适配，可用菜单 `NeuroPilot > Training Room > Integrate Imported Scene`。它会保存训练房间、入口转场目标和构建列表；已有手动场景调整时请先备份。普通打开/运行不需要点此菜单。

## 验证范围与限制

2026-09-06 编辑器端到端验证结果：PASS。已检查训练与结算截图，修正 XR 遮罩遮挡、细字难读和结算内容重叠。

当前修复包为 `NeuroPilotXR_1.1.1_TrainingRoom.apk`，头显应用名显示 `NeuroPilot XR 1.1.1`，versionName `1.1.1` / versionCode `4`。入口顶部必须显示 `v1.1.1 TRAININGROOM`，进入训练后必须显示 `TrainingRoom v1.1.1`；看不到这两个标识就说明运行的仍是旧 APK。

构建结果：Succeeded，0 errors。APK 大小 44,944,422 字节；SHA256：`C7E6FB61F8022F8427E01C496502BE487D673B339F9813FCC504CA3AA8847283`。APK 内嵌场景检查结果：包含 `NeuroPilotNavigation` 和 `TrainingRoom`，不包含 `SpaceTraining`。

安装修复包后请在头显应用列表确认名称。如果覆盖安装仍显示旧名称，先运行 `adb uninstall com.neuropilot.xr`，再重新安装；卸载会删除该应用的本地数据。

- [训练房间预览](Assets/NeuroPilot/Previews/TrainingRoom_v1.1.png)
- [结算预览](Assets/NeuroPilot/Previews/TrainingRoom_Result_v1.1.png)

自动验证工具：`TrainingRoomVerification.Run`，覆盖入口按钮转场、场景摄像机/XR Origin 数量、强度传递、源参数、出球、键盘命中、漏失、结算和重新开始。计时加速只修改运行时配置副本，不修改源配置资产。

预览截图来自编辑器，不代表头显实际佩戴效果。手柄硬件输入、头显重新定位、实际 MR 合成和刺激时序仍需真机验收。APK 为开发测试版，不是商店发布版；既有 Android 15 的 16 KB 对齐警告需要在后续商店发布前处理。
