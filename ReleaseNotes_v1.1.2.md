# NeuroPilot XR 1.1.2 — 固定前方单球训练

VIVE Focus Vision Android 开发测试版；包名 com.neuropilot.xr，versionCode 5。基于 1.1.1 白色 TrainingRoom 更新。

- 小球在入场确定的固定前方区域逐个出现，转头不移动靶区。合入材料的 3.5–8.5 米距离，保留其余刺激和计分参数。
- 顶部命中数、剩余时间、命中率三块统计；中央准备倒计时结束后清空，统一训练提示和结算卡。
- 白灰房间与较暗前墙突出目标。按用户要求保留手柄和射线，不添加人物手臂、枪械。
- 保留入口空间窗口的点击、拖动和启动定位。旧星际场景不进入 APK，工具不再回退到它。
- 新增 TrainingEegPort：预留 Python WebSocket 8765 配置、目标启停事件和目标编号校验的命中入口。默认不联网，无真实 EEG 分类；扳机 / 空格仍为模拟命中。
- 单球、180 秒、15 Hz 配置值、0.5 占空比、12 秒最长暴露保持不变。多球、完整 EEG 网络、刺激参数和玩法大改均需另行确认。

已进行 Unity 编辑器端到端自动验证、600 次固定靶区采样、头部转动不改变靶区、UI 文字与手柄/射线启用状态检查、命中/漏失/结算/重开和本地 EEG JSON 接口回归。另提供准备、训练和结算截图。

安装附件 NeuroPilotXR_1.1.2_TrainingRoom.apk；同签名旧版可覆盖安装。教程见仓库《1.1.2白色房间更新与脑电接口.md》。安装后确认显示 1.1.2。

没有连接头显和脑电硬件：实际佩戴体验、硬件交互、MR 合成和刺激时序尚待真机验收。逻辑事件不等于硬件打标，15 Hz 配置不代表已实测物理频率。现有 SDK 存在 Android 15 16 KB 对齐警告，本包不是商店正式版。
