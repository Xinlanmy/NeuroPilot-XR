# NeuroPilot XR 2.2：L2 眼动门控子集闪烁

以 2026-09-12 定稿的 openspec `add-core-system` 决策 10 为准：**眼动决定"谁闪"（门控），SSVEP 决定"打谁"（确认）**。
2.2 起多球模式不再是"三球齐闪"，而是"注视起闪 + 邻域 ≤3 同闪"；**齐闪降级为眼动失效的备用模式**。
2.1 的独立眼动模式（视线追踪）与单球模式不变。

## 交互流程

1. 房间同屏 **6 颗球**，初始为固定两行三列（相邻角距约 21°/15°，满足 L2「同屏任两目标 ≥10–15°」的生成约束）。
2. **注视任一球 0.3 秒** → 以该球为锚点，取判定范围内（球心距 ≤2.4 米、与视线夹角 ≤50°）**最近的 ≤3 颗**同时闪烁，锚点必定在内。
3. 三颗球各自 **从频率池 12/10/8 Hz 取一个互不相同的频率**，随 `stimulus_onset` 下发；episode（起闪→判定/超时）内频率冻结。
4. 脑电判定仍只在算法侧：判出的频率必须等于该球正在闪的频率才开枪。**注视不参与得分**。
5. 命中后球隐藏、星爆 + 提示音，0.35 秒后在**限定区域内随机复现**（x 在入场原点 ±1.9 米、y 在 −0.4~+1.0 米、z 在 +4.1 米，且与其它球 ≥0.9 米，不重叠）。

## 与 2.1 的差别

| | 2.1 多球 | 2.2 多球（L2） |
| --- | --- | --- |
| 起闪 | 倒数结束三球齐闪，不依赖眼动 | 注视 dwell 0.3 秒才起闪，注视外不闪 |
| 频率 | 槽位绑定（目标 1/2/3 = 12/10/8） | 池内贪心 max-min 取号，随 onset 下发，同闪两两不同频 |
| 离场 | 不涉及 | 视线离开 0.5 秒迟滞后才停闪，迟滞期内不还频率 |
| 眼动 | 房间内销毁眼动组件 | 房间内启用眼动，仅用作门控 |
| 球数 | 3 | 6（同时闪烁仍 ≤3） |
| 失效 | — | 眼动无效 3 秒自动转齐闪，恢复自动切回 |

## 门控参数（权威值在场景，`configs/eeg_w64.toml` 的 `[ssvep.gate]` 是镜像）

| 参数 | 值 | 说明 |
| --- | --- | --- |
| `dwellSeconds` | 0.3 | 注视起闪 |
| `leaveHysteresisSeconds` | 0.5 | 离开迟滞，期内不还频率、不关判定窗 |
| `rampSeconds` | 0.4 | 渐入渐出（< 0.45 秒的算法 guard，不进判别窗） |
| `maxSimultaneous` | 3 | 同闪上限 = 池大小 12/10/8 |
| `neighborRadiusMeters` / `maxViewAngleDeg` | 2.4 / 50 | 邻域范围 |
| `gazeLossFallbackSeconds` | 3 | 眼动失效多久转齐闪（0 = 关） |
| `rearmSeconds` | 6 | 在闪超时未命中原地再武装（0 = 关）；必须 < 闭环 `--deadline` 8 秒 |
| `policy` | GazeGated | 场景内可定死为 `AlwaysOn`（纯齐闪 = 2.1 行为） |

## 给算法队友的接口（协议字段零新增）

- 只有**门控子集**内的球发 `stimulus_onset`，未起闪的球不发任何事件；`freq_hz` 语义是"池内动态取号"，不再绑定槽位。
- `stimulus_offset` = 判定窗关闭时刻（渐出开始），视觉残留 ≤ `rampSeconds`。
- **超时再武装**：同一颗球连续在闪 ≥6 秒未命中 → 旧 id 的 `stimulus_offset` + 新 id 的 `stimulus_onset`（频率与视觉相位不变）。以最新 id 为准，老试次收到 offset 即 abort。
- 服务端（`app/server/fusion/closed_loop.py`）会**拒装同频 onset**并打日志：同频两个球会被一段 EEG 一起打掉，频率分配器在前端、这道护栏在后端。判定行新增 `in_play`（决策时刻在闪的频率集合）供复盘举证。
- 启动闭环：`uv run python -m app.server.fusion.closed_loop --config configs/eeg_w64.toml`（频率池 12/10/8；`--web` 可同时开实时数据台）。

## 验收

- 软件：`NeuroPilotXR.Editor.ThreeModeVerification.Run`（batchmode 不带 `-quit`）覆盖——注视前不闪、dwell 只点亮邻域 ≤3、频率两两不同、迟滞期内不还频率且回头不抖动、池满抢占、超时再武装换新 id 同频、眼动不得分、单条合法 `command_fire` 只计一次、复现在限定区域内且不重叠、眼动失效降级齐闪并能恢复。
- 契约：`python Tools/validate_release.py`（版本 2.2.0 / versionCode 11、门控组件与参数、6 球与复现区域）。
- 真机（**未验证**）：串流下眼动可用性决定门控能否成立；需实测"只有注视频率对应的球消失"、不匹配频率不开枪、一轮 ≥10 靶、眼动失效 3 秒内降级且恢复 ≤30 秒。

## 已知边界

- 频率池只有 3 个而同闪上限也是 3，三球同时在闪时 12 Hz 的 2 次谐波(24 Hz) 与 8 Hz 的 3 次谐波(24 Hz) 碰撞**无法靠取号规避**；若真机出现 12/8 混淆，把 `maxSimultaneous` 降到 2（字段已在，不改代码）。
- 时长与阈值仍由算法侧决定（窗长 2 秒、连续 2 窗、超时 8 秒）；一次成功判定需要 3–5 秒注视，注视提前离开即 abort，这是设计行为不是缺陷。
- Editor 模拟眼动只在 `UNITY_EDITOR` 生效，不会编入 APK；软件验证通过不代表真机验收通过。
