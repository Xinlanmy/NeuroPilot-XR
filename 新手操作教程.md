# NeuroPilot XR 新手操作教程

已更新为 1.1：训练场景已接入。安装和训练操作请优先阅读 [1.1 场景接入与验收](1.1场景接入与验收.md)。

这份教程按“完全不熟悉 Unity 也能照着做”的方式编写。当前项目已经由 Unity MCP 自动搭好，你不需要重新创建 Canvas、XR Origin、按钮，也不需要手动粘贴代码。

项目目录：`D:\workspace\NeuroPilotXR`

Unity 版本：`2022.3.62f1`

## 一、用户最终会看到什么

在 VIVE Focus Vision 上启动应用后，彩色 Passthrough 会显示真实房间；用户正前方约 1.5 米处会叠加一块横向 4:3 的 Unity 空间窗口：

1. 首先显示“NeuroPilot XR / 注意力训练中心 / 沉浸式脑机注意力训练系统”。
2. 用控制器射线点击“进入训练”。
3. 原内容淡出，仍在同一块窗口内显示“轻度 / 标准 / 挑战”。
4. “标准 Standard”默认选中；用户也可以切换为其他强度。
5. 用射线指向窗口顶部的蓝色横条，按住食指扳机并移动手柄，可以移动整个窗口；松开扳机后窗口停在空间里。
6. 点击“开始训练”，出现准备倒计时，现实画面与 UI 柔和淡出，然后进入全沉浸式 `TrainingRoom` 场景。

当前 `TrainingRoom` 来自用户提供的 `D:\workspace\11`，包含白色训练房间、闪烁球、计时和结算。旧 `SpaceTraining` 保留为历史占位，不参与构建。

当前视觉版本以体验和美学为优先：深海军蓝底、双层冷蓝亮边、醒目的大标题和单一高亮主按钮。它没有依赖高成本的实时玻璃折射，而是用预生成渐变、轻量辉光和交互缩放来获得稳定效果，更适合 Android VR 头显。

## 二、项目里已经创建了什么

主要场景：

- `Assets/NeuroPilot/Scenes/NeuroPilotNavigation.unity`：应用入口和空间导航界面。
- `Assets/NeuroPilot/TrainingRoom/Scenes/TrainingRoom.unity`：当前训练房间。

主要脚本：

- `NavigationController.cs`：欢迎页和难度页的淡入淡出。
- `DifficultySelector.cs`：保证三个难度只能选一个，并默认选择 Standard。
- `DifficultyCardView.cs`：卡片选中、悬停、按下反馈。
- `SpatialWindowDragController.cs`：顶部 DragArea 的抓取、缩放反馈和安全距离限制。
- `SpatialWindowInitialPlacement.cs`：每次启动及从后台返回入口时，等待头显定位后，将窗口放在当前正前方约 1.5 米处；摆好后不随头部移动。
- `SceneTransitionManager.cs`：倒计时、视野淡出和统一切换场景。
- `TrainingSession.cs`：跨场景保存当前难度。
- `TrainingHUD.cs`：在训练房间显示强度、计时、命中和结算；旧 `TrainingScenePresenter.cs` 仅用于历史占位场景。
- `UIInteractionFeedback.cs`：按钮柔和缩放动画。
- `VivePassthroughManager.cs`：在导航场景启动 VIVE 全视野 Underlay Passthrough，并在训练开始前关闭。

已经接好的 XR 基础设施：

- XR Origin（XR Rig）
- 左右手控制器与射线交互器
- XR Interaction Manager
- Input Action Manager
- EventSystem + XR UI Input Module
- World Space Canvas + Tracked Device Graphic Raycaster
- OpenXR Loader
- VIVE XR Passthrough（Full-field / Planar Underlay）
- VIVE Focus、VIVE Cosmos、Vive Controller、Oculus Touch、Valve Index 和 KHR 通用控制器配置

## 三、第一次打开项目

### 第 1 步：从 Unity Hub 添加项目

1. 打开 Unity Hub。
2. 点击左侧“项目”或 `Projects`。
3. 点击右上角“添加”或 `Add`。
4. 选择磁盘中的项目。
5. 选择文件夹：`D:\workspace\NeuroPilotXR`。
6. 确认使用 Unity `2022.3.62f1` 打开。

第一次打开会导入 URP、OpenXR、XR Interaction Toolkit、VIVE OpenXR 2.5.1 和中文字体，等待时间可能较长。右下角进度条消失后再操作。

### 第 2 步：确认没有编译错误

1. 在 Unity 顶部点击 `Window > General > Console`。
2. 看 Console 中是否有红色条目。
3. 没有红色错误就可以继续。
4. VIVE 包偶尔会输出控制器注册信息；只要不是红色 Error，不影响继续测试。

### 第 3 步：打开入口场景

1. 在 Project 窗口依次展开：`Assets > NeuroPilot > Scenes`。
2. 双击 `NeuroPilotNavigation`。
3. Hierarchy 顶部应看到：

```text
Directional Light
XR Interaction Setup
NavigationBackdrop（灰色、未启用）
PassthroughManager
MRNavigationRoot
SceneTransitionManager
```

看到这些对象，说明打开的是正确场景。

## 四、最简单的电脑预览方法

电脑预览主要检查画面和页面逻辑，最终的手柄射线与拖拽必须在头显中验收。

1. 确保当前打开 `NeuroPilotNavigation` 场景。
2. 点击 Unity 顶部中间的三角形 Play 按钮。
3. 点击 `Game` 标签页。
4. 应看到 4:3 悬浮窗口；窗口周围在普通 Editor Game View 中通常仍是黑色。
5. 点击“进入训练”，应显示三张难度卡片。
6. 默认高亮项应是“标准 Standard”。
7. 点击其他卡片时，高亮只能移动到一个卡片上。
8. 点击“开始训练”，等待倒计时后应进入白色 TrainingRoom 训练房间。
9. 测试完成后再次点击 Play 按钮退出播放模式。

普通 Editor Game View 看不到真实房间是正常的，因为摄像头 Passthrough 由 Focus Vision 的 OpenXR Runtime 在头显里合成。此时黑色只代表“Editor 没有真实摄像头层”，不是虚拟宇宙背景。场景中的蓝球、红球和星点已经全部禁用。

如果鼠标不能触发按钮，不代表 VR 射线失效。当前 EventSystem 主要服务 XR 射线；连接头显后按下一节测试。

## 五、头显中的六步验收

### 测试前检查

1. 连接头显并启动对应的 OpenXR Runtime。
2. 确保左右控制器已经连接。
3. 在 Unity 中打开 `NeuroPilotNavigation`。
4. 点击 Play，或安装后直接从头显启动 APK。
5. 戴上头显后面向正前方并完成一次视角重置/回正。

### 动作 1：看见欢迎页

应该看到：

- 窗口在视线附近，距离约 1.5 米。
- 窗口周围能直接看见真实房间，而不是黑色宇宙、星星或彩色球体。
- 窗口不贴脸，也不铺满全部视野。
- 标题是“注意力训练中心”。
- 主窗口是横向 4:3，不是竖屏或超宽屏。

### 动作 2：拖动窗口

1. 用控制器射线指向窗口最顶部的细蓝色横线。
2. 射线必须落在顶端窄条区域，不要指向正文或按钮。
3. 按住食指下方的扳机键（与点击“进入训练”使用同一个按键）。只有震动表示指到了顶部，还需要按住扳机才算抓住。
4. 保持按住，缓慢向左右、上下或前后移动控制器。
5. 整块窗口应跟随移动，并有非常轻微的放大反馈。
6. 松开扳机，窗口应停留在新位置。

安全限制已经写入脚本：窗口不会被放到离头部过近的位置，也限制了极端高度。

重开位置验收：先把窗口拖到左侧，再退出应用；转向另一面后重新启动。窗口应重新出现在你当前正前方约 1.5 米处，中心略低于眼睛，不会恢复刚才拖放的位置。从系统主页返回仍在运行的入口页面也会重新定位。正常转头时窗口仍固定在真实空间中。

### 动作 3：进入训练选择页

1. 用射线指向“进入训练”。
2. 按一下 Select/扳机键，不要长按。
3. 欢迎内容应淡出。
4. 三张难度卡片应淡入。
5. 窗口在世界空间的位置不能跳动。

### 动作 4：选择难度

1. 初始状态检查“标准 Standard”是否高亮并显示“已选择”。
2. 点击“轻度 Beginner”，高亮应移动到左卡片。
3. 点击“挑战 Advanced”，高亮应移动到右卡片。
4. 任意时刻只能有一个卡片显示“已选择”。

界面不会显示 Hz、采样率、Trial 时长或 EEG 通道；这里只保存 Beginner、Standard、Advanced 三档产品级选择。

### 动作 5：开始训练

1. 点击底部“开始训练”。
2. 三张卡片应淡出。
3. 中央应出现“正在准备训练...”和 3、2、1。
4. 视野随后柔和淡出，不应瞬间硬切黑屏。

### 动作 6：进入训练房间

应该进入 `TrainingRoom`，并看到：

- 白色房间、倒计时和闪烁球。
- “当前训练强度：轻度/标准/挑战”中的一种。
- 这里显示的强度必须和上一步选择一致。

六项都通过，入口导航 MVP 才算验收完成。

## 六、Hierarchy 和 Inspector 已经怎么连接

这些引用已经自动接好，不需要你重新拖拽。下面用于你排错时核对。

### MRNavigationRoot

挂有：

- `DifficultySelector`
- `NavigationController`

`NavigationController` 的引用：

- Welcome Page → `GlassNavigationPanel/WelcomePage`
- Difficulty Page → `GlassNavigationPanel/DifficultyPage`

`DifficultySelector` 的 Cards 数组：

- BeginnerCard
- StandardCard
- AdvancedCard

默认难度是 `Standard`。

### GlassNavigationPanel

主要组件：

- Canvas，Render Mode 为 `World Space`
- Tracked Device Graphic Raycaster
- Rigidbody，关闭重力并使用 Kinematic
- XR Grab Interactable
- Spatial Window Drag Controller
- Spatial Window Initial Placement

关键数值：

- RectTransform：`1200 × 900`，严格保持 4:3。
- 世界缩放：`0.001`。
- 运行时初始位置：当前头显位置 + 水平朝向 × `1.5` 米，中心比眼睛低约 `0.10` 米。编辑器中保存的 Position 仅用于布局预览。
- XR Grab Interactable 的 Movement Type 为 `Instantaneous`，禁用抛掷及旋转跟踪；窗口保持竖直。
- XR Grab Interactable 的 Colliders 中只有顶部 `DragArea`，所以点击正文和按钮不会误触拖动。

### SceneTransitionManager

挂有 `SceneTransitionManager` 脚本。训练场景名称只在这里配置一次，当前值是 `TrainingRoom`；Passthrough Manager 引用已经连接到场景根对象 `PassthroughManager`。

### PassthroughManager

挂有 `VivePassthroughManager`：

- 场景启动后等待 OpenXR Loader。
- 创建 Full-field Planar Passthrough。
- Layer Type 固定为 `Underlay`，所以现实画面在 UI 后方。
- 训练开始时先降低 Passthrough Alpha，再销毁 Passthrough Handle。
- 普通 Editor 没有设备 Runtime 时只输出提示，不会创建假的背景图片。

XR Main Camera 已设为：

- Clear Flags：Solid Color
- Background：RGBA `(0, 0, 0, 0)`

`NavigationBackdrop` 保留用于回滚，但根对象处于未启用状态；不要勾选它。

## 七、只修改文字时怎么做

以修改副标题为例：

1. 退出 Play 模式。
2. 在 Hierarchy 展开：`MRNavigationRoot > GlassNavigationPanel > WelcomePage`。
3. 点击 `Subtitle`。
4. 在 Inspector 找到 `TextMeshPro - Text (UI)` 组件。
5. 修改最上方的 Text 内容。
6. 按 `Ctrl + S` 保存场景。
7. 点击 Play 检查。

修改其他文字也是相同方法。不要删除或替换 `NotoSansSC SDF` 字体，否则中文可能变成方框。

## 八、调整窗口位置和大小

### 调整初始距离

1. 退出 Play 模式。
2. 选中 `MRNavigationRoot > GlassNavigationPanel`。
3. 在 Inspector 找到 `Spatial Window Initial Placement` 组件。
4. 修改 `Initial Distance`，`1.5` 表示距当前头显正前方约 1.5 米。
5. 建议只在 `1.3` 到 `1.8` 之间调整。
6. 按 `Ctrl + S` 保存。

### 调整高度

- 修改 `Spatial Window Initial Placement` 的 `Eye Height Offset`。
- 默认 `-0.10`，表示比当前眼睛低 10 厘米，会自动适应坐姿和站姿。
- 不要用 Rect Transform 的 Position Y 调整运行时初始高度，因为启动定位会重新计算位置。

### 调整视觉大小

- 当前 Scale 是 `0.001`。
- 想略微放大，可改为 `0.0011`。
- 想略微缩小，可改为 `0.0009`。
- X、Y、Z 三个 Scale 必须保持相同。
- 不要修改 `1200 × 900` 的比例；否则会破坏 4:3。

## 九、修改颜色时怎么做

1. 退出 Play 模式。
2. 展开 `GlassNavigationPanel`。
3. 选择 `GlassBackground`、`EdgeHighlight`、`InnerGlow` 或 `TopAccent`。
4. 在 Image 组件中修改 Color。
5. 每次只改一个对象，播放检查后再改下一个。

为了保持可读性：

- GlassBackground 不要把 Alpha 降到太低。
- EdgeHighlight 应比背景亮，但不要做高饱和霓虹闪烁。
- 透明 UI 层数不要继续大量增加，Android VR 对透明叠加比较敏感。

## 十、未来如何替换训练内容（当前版本无需操作）

1.1 已接入 TrainingRoom，不需要再导入或改名。下面仅保留未来导入其他场景的说明；若更换场景，应同时更改 SceneTransitionManager 的 Training Scene Name 和 Build Settings 中第 1 项，二者必须一致。

当前新项目没有修改原来的 `D:\workspace\星际巡航VR场景`。建议用导出包方式把正式场景迁入，不要直接在文件管理器里零散复制材质。

### 在旧项目中导出

1. 用 Unity 打开原来的星际项目。
2. 在 Project 窗口找到正式星际训练 Scene。
3. 单击选中该 Scene。
4. 点击顶部 `Assets > Export Package...`。
5. 勾选 `Include dependencies`。
6. 点击 `Export`。
7. 保存为例如 `SpaceTrainingContent.unitypackage`。

### 在 NeuroPilotXR 中导入

1. 关闭旧项目或确认当前正在操作的是 `NeuroPilotXR`。
2. 在新项目点击 `Assets > Import Package > Custom Package...`。
3. 选择刚才导出的 `.unitypackage`。
4. 点击 `Import`。
5. 等待导入和编译结束。
6. 在 Console 中确认没有红色错误。

### 连接真实场景

未来若要替换为另一个名为 `SpaceTraining` 的场景，需要同步修改转场目标：

1. 先备份现有场景；不要覆盖目前的 TrainingRoom。
2. 把正式训练 Scene 改名为 `SpaceTraining`。
3. 打开 `File > Build Settings`。
4. 从构建列表移除原训练场景条目（不是删除场景文件）。
5. 把正式 `SpaceTraining` Scene 拖入 `Scenes In Build`。
6. 确保入口 `NeuroPilotNavigation` 排在第 0 项，正式 `SpaceTraining` 排在第 1 项。
7. 入口 SceneTransitionManager 的 Training Scene Name 改为 `SpaceTraining`，保存后从欢迎页完整测试一次。

如果正式星际场景已经自带 XR Origin、Camera Rig 和 EventSystem，先不要重复添加第二套。一个 Scene 中通常只保留一套活动的 XR Origin 和 EventSystem。入口场景和训练场景分别拥有自己的 XR Rig 是正常的，因为它们不会同时加载。

## 十一、重新自动生成界面

顶部菜单中有：`NeuroPilot > Build Complete MVP`。

它会重新生成入口、重新写入 Build Settings 并重新配置 OpenXR；已存在导入的 TrainingRoom 时会保留它。只有在以下情况使用：

- 场景被误删。
- 引用被拖乱且无法恢复。
- 想回到自动生成的默认版本。

重要：如果你已经手动调整入口布局、颜色，不要随便点击这个菜单。它会重新保存 `NeuroPilotNavigation`，可能覆盖入口修改；TrainingRoom 不存在时才生成占位场景。使用前先复制场景做备份。

## 十二、构建 Android APK

项目已经设置：

- 包名：`com.neuropilot.xr`
- Scripting Backend：IL2CPP
- Architecture：ARM64
- Minimum API Level：Android 10 / API 29
- OpenXR Loader：已配置

### 图形界面构建

1. 退出 Play 模式。
2. 点击 `File > Build Settings...`。
3. 左侧选择 `Android`。
4. 点击 `Switch Platform`，等待重新导入结束。
5. 检查 `Scenes In Build`：
   - 第 0 项：NeuroPilotNavigation
   - 第 1 项：TrainingRoom
6. 点击 `Player Settings...`。
7. 在 `Other Settings` 中核对：
   - Active Input Handling 使用 Input System。
   - Scripting Backend 为 IL2CPP。
   - Target Architectures 只需 ARM64。
8. 回到 Build Settings。
9. 点击 `Build`。
10. 保存到：`D:\workspace\NeuroPilotXR\Builds\Android\NeuroPilotXR_1.1.1_TrainingRoom.apk`。
11. 等待 Console 显示 Build completed。

当前 Unity 2022.3 + OpenXR 1.12.1 + VIVE OpenXR 2.5.1 构建时可能提示“可选输入 API 迁移”和“Android 15 16KB page alignment”警告。它们没有阻止本次 APK 生成；做 Focus Vision 内部测试可以继续。以后若要上架面向 Android 15+ 的商店版本，再统一升级 Unity/OpenXR/VIVE 原生插件，不要在当前交互验收阶段混装另一套 SDK。

### 安装到头显

先打开头显开发者模式和 USB 调试，再连接 USB。这台电脑当前 Unity 实际使用的 adb 路径是：

```text
D:\UnityAndroidTools\2022.3\SDK\platform-tools\adb.exe
```

在 PowerShell 中执行：

```powershell
& 'D:\UnityAndroidTools\2022.3\SDK\platform-tools\adb.exe' devices
```

如果设备列表出现一串设备编号和 `device`，再执行：

```powershell
& 'D:\UnityAndroidTools\2022.3\SDK\platform-tools\adb.exe' install -r 'D:\workspace\NeuroPilotXR\Builds\Android\NeuroPilotXR_1.1.1_TrainingRoom.apk'
```

出现 `Success` 就表示安装完成。然后在头显应用库的“未知来源/开发者应用”中打开 NeuroPilot XR。

### VIVE Focus Vision 真机 Passthrough 验收

1. 安装完成后先站在安全、光线充足的房间。
2. 启动 `NeuroPilot XR`。
3. 如果系统弹出边界、空间或摄像头相关授权，按头显提示允许。
4. 应先看到真实房间的彩色摄像头画面。
5. 真实房间前方应叠加“注意力训练中心”窗口。
6. 缓慢转头：真实环境随头部视角变化；UI 留在原来的世界位置。
7. 用控制器拖动顶部 DragArea：UI 在真实房间中移动。
8. 点击开始训练：真实环境逐渐消失，然后进入 TrainingRoom 的纯 VR 画面。

不要用 Unity Game View 的黑底来判定 Passthrough 失败。最终判定只看 Focus Vision 真机。

### 可选：Direct Preview

如果电脑已经正确安装并配置 VIVE Business Streaming / VIVE Direct Preview，可以在 `VIVE` 菜单中打开对应的 Direct Preview 设置后连接头显，再从 Unity 点击 Play。不同电脑和 Runtime 的预览兼容性可能不同；如果 Direct Preview 中仍无摄像头画面，以 Android APK 真机结果为准。

## 十三、最常见问题

### 1. 头显里完全看不到窗口

依次检查：

1. 当前 Scene 是否为 `NeuroPilotNavigation`。
2. `GlassNavigationPanel` 左侧勾选框是否开启。
3. `Spatial Window Initial Placement` 是否开启、Viewer 是否引用 Main Camera；戴好头显并等待定位完成。
4. Scale 是否接近 `(0.001, 0.001, 0.001)`。
5. 在头显中执行一次视角回正。

### 1A. 头显里有窗口，但周围仍是黑色

依次检查：

1. 使用的是 VIVE Focus Vision Android APK，而不是只看电脑 Game View。
2. `NavigationBackdrop` 必须保持未启用。
3. Hierarchy 中必须存在并启用 `PassthroughManager`。
4. `Edit > Project Settings > XR Plug-in Management > OpenXR` 的 Android 标签中，VIVE Passthrough 功能必须启用。
5. Main Camera 的 Background Alpha 必须为 0。
6. Console/Android Logcat 中搜索 `VIVE passthrough`；若 Runtime 返回失败，先确认头显系统和 VIVE Runtime 已更新，并重新启动头显。

### 2. 能看到射线，但按钮点不了

检查：

1. Hierarchy 是否只有一个 EventSystem。
2. EventSystem 是否带 `XR UI Input Module`。
3. GlassNavigationPanel 是否带 `Tracked Device Graphic Raycaster`。
4. 控制器是否被当前 OpenXR Runtime 正常识别。
5. Project Settings > XR Plug-in Management 中当前平台是否勾选 OpenXR。

### 3. 按钮能点，但窗口拖不动

1. 必须指向最顶部的细横线区域。
2. 不要指向正文、卡片或按钮拖动。
3. XR Grab Interactable 的 Colliders 列表里应只有 `DragArea` 的 Box Collider。
4. 安装本次更新的 APK，指向顶部并按住食指扳机；旧版使用侧握键，操作映射不同。
5. 导航场景左右手 Controller 的 Select Action 和 UI Press Action 应引用同一个 `UI Press` 动作。

### 4. 中文变成方框

1. 选中出问题的文字对象。
2. TextMeshPro 组件的 Font Asset 应为 `NotoSansSC SDF`。
3. 不要改用只含英文字形的默认 LiberationSans SDF。

### 5. 点击开始训练后提示场景不存在

1. 打开 `File > Build Settings`。
2. 确认 `TrainingRoom` 在 Scenes In Build 中并且已勾选。
3. 确认 SceneTransitionManager 的 Training Scene Name 与场景名完全一致。
4. 名称区分空格和拼写；不要写 `.unity` 后缀。

### 6. 材质变成粉色

1. 打开 `Edit > Project Settings > Graphics`。
2. Scriptable Render Pipeline Settings 应指向 `NeuroPilotURP`。
3. 打开 `Edit > Project Settings > Quality`，当前质量等级也应使用同一 URP Asset。

### 7. Android 构建失败

先检查：

- Unity Hub 中是否给 2022.3.62f1 安装 Android Build Support、SDK、NDK 和 OpenJDK。
- Console 最上方第一条红色错误；后面的错误往往只是连锁反应。
- 场景是否已保存。
- 是否仍在 Play 模式。

## 十四、哪些东西现在不要动

在完成头显六步验收前，建议不要：

- 删除 XR Interaction Setup。
- 再创建第二个 EventSystem。
- 把 World Space Canvas 改成 Screen Space Overlay。
- 给整个面板添加新的大 Collider。
- 把窗口每帧绑定到 Camera。
- 勾选 `NavigationBackdrop`，否则会重新出现虚拟星空和球体。
- 把 Passthrough 从 Underlay 改成 Overlay，否则现实层可能覆盖 Unity UI。
- 把 MR 导航 Camera 的背景 Alpha 改回 1。
- 删除 NotoSansSC 字体资源。
- 同时启用两套不同的输入系统。
- 添加昂贵的实时高斯模糊、多个 RenderTexture 或复杂折射。

## 十五、最终验收清单

请逐项打勾：

- [ ] 启动后能看见“注意力训练中心”。
- [ ] Focus Vision 中能看见真实房间，周围没有蓝球、红球和星空。
- [ ] 主窗口是横向 4:3。
- [ ] 顶部 DragArea 能拖动整块窗口。
- [ ] 左右手都能按住食指扳机拖动，松开后窗口固定在新位置。
- [ ] 换一个朝向重新启动应用后，窗口出现在当前正前方，不保留上次拖放位置。
- [ ] 正文和按钮区域不会误触拖动。
- [ ] 点击“进入训练”后在同一窗口显示难度页。
- [ ] Standard 默认选中。
- [ ] 三个难度可以互相切换，且始终只有一个选中。
- [ ] 点击“开始训练”后出现倒计时，Passthrough 与 UI 柔和淡出。
- [ ] 能进入 TrainingRoom。
- [ ] TrainingRoom 显示的难度与用户选择一致。
- [ ] 扳机/空格可模拟命中，超时计漏失，结算后侧握键/R 可重开。
- [ ] Console 没有红色运行错误。

全部通过后，再开始接正式星际内容和真实 SSVEP 训练逻辑。
