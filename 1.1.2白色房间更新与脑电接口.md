# 1.1.2 固定前方训练区与脑电接口

基于 v1.1.1（a9152b275d86ad752595b764426a11abf1b91ba6）更新。用户确认：单球逐个出现、固定前方靶区、Aim Lab 参考 UI；训练房间保留手柄与射线，不添加人物手臂和枪械。

## 本版行为

- 入场时一次性对齐房间并确定前方训练坐标。之后转头、移动头部或重开本轮，都不会改变靶区；退出再进入场景会重新对齐。
- “身前”指入场朝向建立的训练区，不是身体追踪或眼动注视点测量。转身时小球可以暂时不在视野内，这是正常行为，不会把下一球搬到新视线前。
- 合入材料的 3.5–8.5 米深度，横向在固定前方 ±22°及房间边界内，世界高度仍为 1.3–2.2 米。当前入场 Z=2.5，目标中心 Z=6–11，前墙 Z=12。边界检查包含球半径；尽量与上一球间隔 1.2 米。
- 原材料目录未修改。其 Spawner.cs 仍有世界 Z 钳制问题；本版以固定入场坐标替换。此前按每次头部朝向出球的候选实现已废止，不发布。
- 顶部显示命中数、剩余时间、命中率；中央准备倒计时结束后消失；底部显示简短提示和真实输入模式；结束时显示统一统计卡。
- 命中率 = 命中 /（命中 + 漏失），不是射击准确率或 EEG 置信度。尚无完成目标时显示“—”；无命中时结算平均反应时间显示“—”。
- 白灰房间的前墙降低亮度突出白球，不引入棋盘格、额外闪烁或装饰粒子。手柄和射线保留。
- 保留 180 秒、3 秒准备、15 Hz 配置值、0.5 占空比、单球最长 12 秒及反馈时长。三档强度仍只传递和显示选择。扳机 / 空格模拟命中，结算后侧握键 / R 重开。
- 旧星际场景不参与 APK 构建，也不再是工具的回退选项；历史资产暂留方便追溯。

## 队友更新教程

1. 保存工作并退出 Unity。有未提交修改时先提交自己的分支，不要直接覆盖。
2. 在 Xinlanmy/NeuroPilot-XR 本地目录拉取最新 main。复现本版用 v1.1.2 标签；后续开发从最新主线创建分支。
3. Unity Hub 添加包含 Assets、Packages、ProjectSettings 的仓库根目录，用 2022.3.62f1c1 打开并等待导入完成。
4. 打开 Assets/NeuroPilot/Scenes/NeuroPilotNavigation.unity，Play 后点击进入训练、开始训练，应进入白色房间。
5. 白色场景：Assets/NeuroPilot/TrainingRoom/Scenes/TrainingRoom.unity。
   出球代码：同目录树 Scripts/Core/Spawner.cs。
   参数：Config/SessionConfig_Default.asset。
   HUD：Scripts/UI/TrainingHUD.cs。
   布局工具：Assets/NeuroPilot/Editor/TrainingRoomPresentation.cs。
6. 以后只比较并合入队友修改的对应文件，带上 .meta。不要用 D:\workspace\11 的整份工程覆盖，不复制其 Library、Packages、ProjectSettings 或自动重建场景的 Editor 工具。
7. 重新应用房间布局前先提交备份，再执行 NeuroPilot > Training Room > Integrate Imported Scene。它会保存场景、更新布局/材质和构建设置，普通脚本更新不必反复执行。
8. Build Settings 只有启用的 NeuroPilotNavigation 和 TrainingRoom，前者第一。Android 测试包输出 Builds/Android/NeuroPilotXR_1.1.2_TrainingRoom.apk。

publieople/neuropilot-xr/app/unity/NeuroPilotXR 是以前导入的固定副本，不会自动同步。Unity 日常实现以 Xinlanmy 仓库为准，本次不修改第二个仓库。

## 脑电接口预留：默认不联网

SessionManager 上的 TrainingEegPort 仅为后续接入点。本版没有 WebSocket 客户端、采集驱动、分类算法或真实脑电识别。填写地址不会建立连接。

| 配置 / 接口 | 用途 |
| --- | --- |
| serverHost | 默认空；未来 APK 填采集电脑局域网 IP，不能用头显自己的 127.0.0.1 |
| serverPort | 默认 8765，Python 融合层 WebSocket 端口；不是 Unity 监听原始脑电 |
| eegInputEnabled | 默认 false；未来适配器联调时显式启用，才消费 EEG 命中并停止使用手动模拟计分 |
| OutgoingMessage | 主线程事件，输出 stimulus_onset / stimulus_offset JSON；无订阅者时不联网、不保存日志 |
| TryReceiveCommand(json) | 主线程调用，只接受当前待命中目标的 command_fire |
| ResetConnectionSequence() | 未来适配器在新连接建立后重置下行序号，不替代连接管理 |
| 心跳常量 | 1 秒发送、3 秒超时的约定；本版只有常量，没有实际心跳 |

信封沿用团队 type / ts / seq / payload；ts 为 Unix 毫秒，seq 递增。

```json
{"type":"stimulus_onset","ts":1788652800000,"seq":0,"payload":{"target_id":"每球唯一ID","freq_hz":15.0,"level":1,"wave":1}}
{"type":"command_fire","ts":1788652801000,"seq":0,"payload":{"target_id":"必须原样回传当前球ID"}}
```

单球房间固定 level=1、wave=1。offset 以 target_id 为准，其余字段可能带默认零值。目标 ID 跨轮次不复用；错误/过期目标、重复或倒退序号、非法 JSON 均拒收。命中、超时、重开、整轮结束和组件关闭会结束当前刺激。

command_difficulty、command_auto、command_reset 暂不实现，避免暗中改变玩法。旧 EegHitSource 是历史占位，后续适配请使用 TrainingEegPort。

通知时间是 Unity 逻辑时间，不是屏幕光子时刻或硬件打标。真实接入前仍需网络传输、断线策略、主线程调度、时钟同步及刷新率/刺激实测。原闪烁代码的毫秒周期取整未改，不能把配置 15 Hz 当作已测量合格的物理输出频率。

接口对照团队 master 29b0634f8c4d15a090d3b2a6c01fc804f4da1498 的 app/unity/README.md 和 app/server/fusion/ws_server.py；没有复制其整套算法或私有文档。

## 验证与安装

自动回归入口 NeuroPilotXR.Editor.TrainingRoomVerification.Run。批处理不加 -quit，测试自行退出，结果在 Logs/v1.1.2-verification.txt。

检查范围：入口转场、唯一相机/XR Origin、600 次固定区域采样、转头不改变靶区或当前球、手柄/射线启用状态、倒计时/统计文字实际生成、模拟命中、EEG 本地接口有效/无效命令、漏失、结算和重开。接口测试不是 WebSocket 或真实脑电测试。

应用名 NeuroPilot XR 1.1.2，包名 com.neuropilot.xr，versionCode 5。同签名旧版可覆盖安装，不必主动卸载。安装后确认入口和房间显示 1.1.2。

头显验收：面向训练区等待出球；转头确认球不跟随，转回后仍在原靶区；下一球也在该区域。确认手柄和射线正常、顶部统计不遮球、准备卡在训练开始时消失、结算后侧握键可重开。

本机没有连接头显或脑电设备，真实佩戴体验和刺激时序尚待实测。APK 为开发测试版；当前 SDK 的 Android 15 16 KB 对齐警告仍待单独处理。

## 后续大改先确认

同时多球、完整 EEG 联网及断线策略、刺激时序/频率调整、瞄准命中判定、PC 串流替代 APK、移除 HUD、自适应难度均不在本版范围，实施前需负责人确认。
