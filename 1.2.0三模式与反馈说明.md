# NeuroPilot XR 1.2.0：视觉与脑电分离

以用户 2026-09-08 最后一次澄清为准。先前尝试的“单球眼动 1 秒”和“多球眼动门控”已移除，不是当前玩法。

## 三个并列模式

| 模式 | 场景 | 目标与确认方式 |
| --- | --- | --- |
| 视线追踪 | EyeTrackingRoom | 同时出现 4/5 个彩球；按提示持续观察蓝色目标 5 秒，视觉确认消除 |
| 单球追踪 | TrainingRoom | 一个 SSVEP 球；只由脑电消息确认 |
| 多球定位 | MultiTargetRoom | 三个 SSVEP 球，以三个不同频率同时闪烁；只由脑电消息确认 |

只有视线模式依赖眼动。单球和多球都不要求眼动数据、不会用注视或扳机代替脑电结果。手柄与射线保留，用于导航操作。

欢迎页 → 模式选择 → 各模式设置。模式选择有“返回首页”，设置页有“返回模式选择”；房间也可返回模式选择，选择值独立保留。旧强度页只归属于单球，三档仍保留选择与显示，不擅自改变原刺激参数。

## 如何体验

1. 覆盖安装同签名 `Builds/Android/NeuroPilotXR_1.2.0_TrainingRoom.apk`，启动包含 1.2.0 的应用。
2. 体验视线模式前，在头显系统设置中开启眼动并完成校准。普通电脑没有眼动数据时会显示等待，这不是自动使用头部方向的模拟演示。
3. 视线模式默认 4 个彩球，可切换 5 个。看蓝球持续 5 秒，移开视线、看错误颜色或失去有效跟踪会重新累计；跟踪无效时暂停训练计时。成功后球消散，下一目标出现在新位置。
4. 单球模式不依赖眼动。点击开始后，原 SSVEP 球按原配置闪烁，等待脑电消息；扳机和空格不会产生假命中。
5. 多球模式不依赖眼动。倒数结束后，三个球立即按目标 1=12、目标 2=10、目标 3=8 Hz 同时闪烁，无需注视起闪。每个目标的 ID、确认和补充互相独立。
6. 新模式结算后点“重新训练”；原单球仍支持结算后侧握键/R。所有房间可返回模式选择。

## 成功反馈

确认成功：球面和碰撞体立即消失，原位置散出 12 片金色五角星，约 0.55 秒散开并缩小消失；同时播放约 0.38 秒低音量短奖励音。球体随后补充到对应训练区域。

错误颜色、无效眼动、未知/过期目标 ID、重复消息都不能重复触发奖励。`SuccessReward.soundEnabled`、`volume` 可在 Inspector 调节。音效为程序合成，不使用第三方歌曲或素材。

## 频率来源和边界

已使用账户读取协作仓库 master `a85327d` 的 `app/unity/README.md`、`configs/eeg_w64.toml` 和设计说明。实际 TOML 候选池为 12/10/8/6 Hz，但文档仍标注待复测定稿，且 6 与 12 不应同时使用。

本次按用户最终要求采用**三个同时闪烁、固定不同频率的目标**，使用该配置前三个频率 12/10/8 Hz，不把 6 Hz 放入同时闪烁组。没有采用仓库另一路“眼动门控子集闪烁”设计；用户明确要求脑电与纯视觉模式分离。

配置为 `Assets/NeuroPilot/TrainingRoom/Config/MultiFrequencyPool.asset`：`frequencies` 必须恰好三个有效、不同且无 f/2f 冲突的值，分别对应目标 1/2/3；`prototypeEnabled` 表示启用开发原型，不代表已完成真机验证。单球继续保留原 15 Hz 参数，本次未偷偷修改原 SessionConfig。

请求相位按连续时间计算，显示仍被实际渲染帧采样。软件事件时间戳不是光学起闪或 TB203 硬件打标。本版尚未验证头显实际闪烁频率、眼动精度、音效听感或脑电分类效果，不是已经验证的正式训练设备。

## 给算法队友的接口

**Unity 本地事件接入点与 WebSocket 传输均已实现（`FusionEegBridge` 装配 `FusionLink`）。真实 EEG 分类仍未实现——未连接算法时，SSVEP 房间会等待确认，不会凭空产生命中。不要把 Editor 注入测试的通过当作脑电硬件闭环成功。**

- 单球：`TrainingEegPort.OutgoingMessage` / `TryReceiveCommand(json)`，场景默认启用 EEG 接收。
- 多球：`TargetPracticeSession.OutgoingEvent` / `TryReceiveCommand(json)`。
- 首页右上角新增“设置”：使用手柄数字键盘输入 WSL 通信设备 IPv4，点击“保存地址”。默认 `192.168.1.131`，端口 `8765`。支持退格、清空和恢复默认；恢复默认后也需要点保存。返回首页会丢弃未保存的输入。
- 地址保存在头显本地 PlayerPrefs，正常退出和重启后保留；卸载或清除应用数据后恢复安装默认值。默认地址和端口集中在 `Assets/NeuroPilot/Resources/CommunicationDefaults.json`，不分别写死在各场景。
- 设置页「测试连接」按钮：向 `ws://{IP}:8765` 发起 4 秒 WebSocket 握手探测，结果以模态弹窗提示成功/失败（含超时/拒绝原因与排查提示）；未保存的输入也可直接测试，draft 为空时测已保存地址。
- 单球 `TrainingEegPort.serverHost/serverPort` 与多球 `TargetPracticeSession.serverHost/serverPort` 实时读取同一份 `CommunicationSettings`；`FusionEegBridge` 读取 `CommunicationSettings.Endpoint` 作为 WebSocket 地址，设置页保存后免重启生效。保存地址不代表网络已连接；用「测试连接」按钮验证。
- Android 上 localhost 指头显本身，不是采集电脑；WSL 服务必须通过头显可达的局域网地址和端口开放，网络转发和防火墙需在联调时验证。
- 上行 `stimulus_onset` 含 `target_id/freq_hz/level/wave`；停止/命中/退出发 `stimulus_offset`。多球成功额外发 `scene_event`，`payload.kind=target_confirmed`。
- 下行只处理 `command_fire`，要求正 ts、递增 seq、当前正在闪烁且可确认的目标 ID；命中前锁定目标，避免重入和重复奖励。
- 重新生成目标使用新 GUID；旧确认消息不会击中新球。新连接由适配器调用 `ResetConnectionSequence()`，再在主线程投递消息。
- 心跳 1 秒、3 秒失联告警（日志）、网络收发与主线程调度由 `FusionLink` / `FusionEegBridge` 提供；断线期间上行事件丢弃不补发，每次新连接自动复位下行 seq 基线。`command_auto/difficulty/reset` 尚不处理。

```json
{"type":"stimulus_onset","ts":1790000000000,"seq":0,"payload":{"target_id":"target-guid","freq_hz":12.0,"level":2,"wave":1,"slot":1}}
```

```json
{"type":"command_fire","ts":1790000000100,"seq":1,"payload":{"target_id":"target-guid"}}
```

纯眼动的 `target_onset/target_offset/target_confirmed` 是独立演示事件，不当作脑电刺激事件直接转发。

## 工程与验收

- Unity：2022.3.62f1c1，URP，OpenXR。启用标准 Eye Gaze Interaction Profile，只有视线房间消费其有效姿态；没有使用头部朝向冒充眼动。
- 三个白色房间是独立场景与会话，不会互相串状态；保留现有手柄、射线、世界空间 UI、入口拖动和启动定位。目标在入场确定的前方区域，不跟随转头。
- 准备：`NeuroPilotXR.Editor.TrainingRoomIntegration.Prepare`。
- 新模式验收：`NeuroPilotXR.Editor.ThreeModeVerification.Run`，batchmode 不带 `-quit`。
- 原单球回归：`NeuroPilotXR.Editor.TrainingRoomVerification.Run`，现已检查空格不能命中、只有脑电确认才命中。
- 打包：`NeuroPilotXR.Editor.TrainingRoomIntegration.BuildAndroid`，带 `-quit`，ARM64 开发包，包名 `com.neuropilot.xr`，versionCode 6。
- 报告：`Logs/three-modes-verification.txt` 和 `Logs/v1.2.0-verification.txt`；截图：`Assets/NeuroPilot/Previews/`。

Editor 模拟眼动只存在于 `UNITY_EDITOR`，不会编入 APK。未连接真实头显/脑电时，不能宣称已完成真机验收。本次本地更新不自动发布 GitHub Release。
